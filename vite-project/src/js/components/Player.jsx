import React from 'react';

export default function Player({ position, width, height, spriteKey, frameIndex, cropWidth }) {
  return (
    <g>
      {/* Placeholder for canvas-rendered player */}
    </g>
  );
}
